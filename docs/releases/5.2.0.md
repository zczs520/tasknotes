# TaskNotes Time 5.2.0

## Added

- Added a Goals settings tab with a configurable goal folder, defaulting to `TASKquence/Tasks/Goals` for new and existing installations. Goal reads, creation, and view refreshes follow the selected folder. Existing notes are not moved automatically; move them into the selected folder or set it to `Goals` to keep using the previous location.
